# WGCNA 输入契约

- 表达矩阵：首列 `gene_id`，其余列为唯一 `sample_id`，所有表达值为数值；说明归一化/变换和低表达过滤。
- 性状表：首列 `sample_id`，其余列为性状或协变量；分类变量需明确编码，连续变量需说明单位。
- 样本必须一一对应。建议至少 15 个样本；更少时应明确作为高风险探索分析。
- 记录物种、组织、批次、实验设计、基因 ID 类型及注释版本。
