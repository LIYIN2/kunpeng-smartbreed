# 鲲鹏智能体 v2.4.6

## 本次更新

- 修复附件卡片右上角 × 在新会话或本地删除请求延迟时无响应的问题。点击后会立即从当前草稿移除附件和引用；临时文件清理由后台完成。
- 修复图片或文件发送后仍残留在下一条输入框的问题。进入生成状态后，本轮已提交附件会从输入框清空，但仍保留给正在执行的本轮任务读取。
- 修复已删除附件可能被后续会话从历史上传目录重新检索的问题；上传文件只允许在其明确附带的当前消息中使用。
- 附件卡片改为左侧图标或缩略图、右侧文件名与大小的紧凑布局。
- 内置 DeepSeek Vision 网关。选择 `DeepSeek + Vision` 下的 `DeepSeek-V4-Flash-Vision-Exp` 后，可使用已有 DeepSeek 凭据读取图片；纯文本 `DeepSeek-V4-Flash` 保持不变。
- 保留对话框局部文件拖拽，移除会造成页面导航或输入框消失的全局拖拽处理。

## 验证范围

- 已执行桌面主进程与发布脚本语法检查。
- Windows NSIS 安装器已实际解包检查，运行时归档、Node 归档和完整预览 ZIP 均通过 7-Zip 完整性测试。
- 随包 DSH 服务已通过本地 HTTP 健康检查；真实模型回答仍取决于用户的 DeepSeek 凭据、网络和所选模型。

## 下载与安装

- `Kunpeng-Agent-2.4.6-x64.exe`：Windows x64 NSIS 安装包。
- `Kunpeng-Agent-2.4.6-win-x64-preview.zip`：完整免安装目录。必须完整解压后，从目录内启动 `鲲鹏智能体.exe`，不要单独复制 EXE。

## 第三方说明

本项目为独立科研软件，与华为技术有限公司及其关联公司无官方关联、合作或商标授权。第三方名称和商标归其权利人所有，详见 [`TRADEMARKS.md`](TRADEMARKS.md)。
