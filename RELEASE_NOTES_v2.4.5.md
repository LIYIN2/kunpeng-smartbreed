# 鲲鹏智能体 v2.4.5

## 本次更新

- 调整运行中发送消息的行为：当用户输入新的临时方向时，客户端先取消当前轮次，确认取消后再发送新内容，避免新消息在旧回复结束后才作为队尾消息出现。
- 技能库支持从 GitHub 一键导入完整技能包；技能的参考资料、示例、资源与脚本会随主技能保留，并可在会话中调用。本次安装包预装当前技能库的 36 个技能，首次启动即可浏览和通过 `/` 调用。
- 优化技能库的卡片浏览、详情查看、分类筛选、排序、分页与删除操作；输入 `/` 可检索并插入技能。
- 修复新会话、临时转向与发送中的消息重复/错位问题；上传文件和图片后不再把本地路径混入对话文本。
- 优化输入框的自适应高度与附件展示，减少空白占位并保持长文本自然扩展。

## 验证范围

- 已执行桌面主进程、技能库与发布脚本的 JavaScript 语法检查。
- Windows NSIS 安装器已实际解包检查：包含 `app.asar`、ICU 数据、Node/DSH 运行时、鲲鹏预设、Univer Office、DeepSeek 配额与中断插件；运行时解包后通过本地 Web 服务 HTTP 健康检查。
- 完整 Windows 预览 ZIP 已通过 7-Zip 完整性测试。
- 登录态下与真实模型服务交互的最终视觉体验仍应以用户实际使用为准；本 Release 不把未做的人工点击测试表述为已验收。

## 下载与安装

- `Kunpeng-Agent-2.4.5-x64.exe` 是 Windows x64 的 NSIS 安装器。
- `Kunpeng-Agent-2.4.5-win-x64-preview.zip` 是完整预览目录。必须解压后从目录内启动 `鲲鹏智能体.exe`，不要单独复制其中的 EXE。

## 第三方说明

本项目为独立科研软件，与华为技术有限公司及其关联公司无官方关联、合作或商标授权。第三方名称和商标归其权利人所有，详见 [`TRADEMARKS.md`](TRADEMARKS.md)。
